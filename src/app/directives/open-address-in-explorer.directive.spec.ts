import { OpenAddressInExplorerDirective } from './open-address-in-explorer.directive';

describe('TransactionExplorerDirective', () => {
    it('should create an instance', () => {
        const directive = new OpenAddressInExplorerDirective();
        expect(directive).toBeTruthy();
    });
});
